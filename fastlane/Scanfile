# For more information about this configuration visit
# https://docs.fastlane.tools/actions/scan/#scanfile

# In general, you can use the options available
# fastlane scan --help

project("BikeIndex.xcodeproj")

scheme("Debug (development)")

# Scanfile contains defaults and these can be overridden by Fastfile
# or CLI arguments
ios_version = "(18.2)"
devices([
	"iPhone 16 #{ios_version}",
	"iPhone 16 Plus #{ios_version}",
	"iPad (10th generation) #{ios_version}"
])

clean(true)

result_bundle(true)

reinstall_app(true)

reset_simulator(true)

force_quit_simulator(true)

include_simulator_logs(false)

number_of_retries(3)
